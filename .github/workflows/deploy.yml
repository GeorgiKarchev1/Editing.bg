name: Deploy to Plesk

# Деактивиран - използваме само Plesk webhook
on:
  push:
    branches: [ main ]
    # workflow_dispatch: # Ръчно стартиране

# Деактивиран - използваме само Plesk webhook
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Skip deployment
      run: echo "Deployment handled by Plesk webhook"
